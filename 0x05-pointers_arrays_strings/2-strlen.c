#include "main.c"
/**
 * int-strlen - string length
 * @a: first char
 * Description: find the lenth of string
 * Return: alwys (0)
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
	continue;
}
return (i);
}
